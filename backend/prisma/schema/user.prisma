model User {
    id           String  @id @default(uuid())
    name         String
    email        String  @unique
    password     String
    refreshToken String?

    tasks Task[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
